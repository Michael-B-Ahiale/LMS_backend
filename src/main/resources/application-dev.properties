# Application Configuration
spring.application.name=lms-backend

server.port=8084
# Server Error Configuration
server.error.include-message=always
server.error.include-binding-errors=always

# JWT KEY
jwt.secret=eW91ci1wbGFpbi1zZWNyZXQta2V5
jwt.expiration=86400
logging.level.org.springframework.security=DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pakyi.lands@gmail.com
spring.mail.password=cxyn xhny gzpy lerb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true





# DataSource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/olms
# spring.datasource.username=postgres
# spring.datasource.password=your-username

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format-sql=true
spring.jpa.show-sql=true

# Spring Session JDBC Configuration
spring.session.jdbc.initialize-schema=always
spring.session.jdbc.table-name=SPRING_SESSION
# spring.session.store-type=jdbc

# Actuator Configuration (Prod-specific settings)
# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=never
# management.endpoint.health.show-components=never